# 배포 안내 (GitHub Pages)

## 공개 주소
**https://effortball-cloud.github.io/rookie-baseball/**

- 로그인 없이 누구나 접속 가능 (친구에게 그대로 공유하면 됩니다)
- 저장소: https://github.com/effortball-cloud/rookie-baseball (Public)
- 배포 방식: GitHub Actions (`.github/workflows/deploy.yml`) — main 브랜치에 푸시하면 자동 배포

## 폴더 구성
| 파일 | 설명 |
|---|---|
| `index.html` | 프로토타입 9개 화면을 하나로 묶은 단일 파일 (약 420KB) |
| `preview.png` | 카톡·SNS 공유 시 뜨는 미리보기 카드 (1200×630) |
| `.nojekyll` | GitHub Pages의 Jekyll 처리 건너뛰기 |
| `.github/workflows/deploy.yml` | 자동 배포 워크플로 |

## 내용을 수정한 뒤 반영하는 방법

프로토타입 원본은 `..\프로토타입\` 폴더에 있습니다. 거기서 파일을 고친 뒤:

### 1) index.html 다시 만들기
```
C:\Users\User\AppData\Local\Programs\Python\Python312\python.exe "<빌드스크립트>\build_pages.py"
```
※ 빌드 스크립트는 프로토타입 9개 HTML을 읽어 `배포\index.html` 하나로 합칩니다.
   (스크립트를 잃어버렸으면 Claude에게 "배포용 index.html 다시 만들어줘"라고 하면 됩니다.)

### 2) 푸시하면 자동 배포
```
cd C:\Users\User\Desktop\스포츠게임\배포
git add -A
git commit -m "내용 수정"
git push
```
푸시 후 1~2분이면 사이트에 반영됩니다.

### 배포 상태 확인
```
& "C:\Program Files\GitHub CLI\gh.exe" run list --limit 3
```

## 메모
- 최초 배포 시 GitHub 기본(legacy) 빌더가 큐에서 멈춰 있어, **GitHub Actions 방식으로 전환**해 해결했습니다. 앞으로도 Actions 방식이 유지됩니다.
- 공유 카드 문구·수치를 바꾸려면 `preview.png`를 다시 만들어야 합니다 (Claude에게 요청).


---

## PWA 부속 파일 (자동 생성 — 직접 고치지 말 것)

`빌드/build_shell.py` 를 돌리면 아래가 함께 만들어진다.

- `manifest.webmanifest` — 앱 정보·아이콘·바로가기
- `sw.js` — 서비스워커. **캐시 이름이 index.html 해시라 빌드마다 바뀐다**
- `robots.txt` · `sitemap.xml`
- 아이콘 4종 (`빌드/make_icons.py` 로 재생성)

빌드 → 커밋 → 푸시 하면 사용자에게 "새 버전이 있어요" 배너가 뜬다.

```
cd 빌드
python make_icons.py     # 로고를 바꿨을 때만
python build_shell.py
python test_pwa.py       # PWA 검증
python test_all.py       # 전체 회귀
```
